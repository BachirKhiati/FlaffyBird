using UnityEngine;
using System.Collections;

public class gpcdestor : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Destroy(GameObject.Find("gpc"));

    }
	
	// Update is called once per frame
	void Update () {
	
	}
}
